# Web API Plan — Day 25

**Spec:** `docs/superpowers/specs/2026-05-30-web-api-design.md`

## Steps

1. **Deps** — add `fastapi`, `uvicorn` to `pyproject.toml`; install into venv.
2. **Failing tests** — `tests/test_web.py` (4 tests) + `feedy serve` test in
   `tests/test_cli.py`. Run, confirm fail.
3. **Implement** — `feedy/web.py` with `app`, `/`, `/digest`.
4. **CLI** — add `serve` command running uvicorn.
5. **Verify** — full suite green.
6. **Roadmap** — mark Day 25 done.

## Commit sequence

- `build: add fastapi and uvicorn dependencies`
- `test(web): add failing tests for digest JSON API`
- `feat(web): add FastAPI app serving digest as JSON`
- `feat(cli): add serve command to run the web API`
- `chore: mark Day 25 complete`
