# XSource Implementation Plan — Day 06 (backfill)

**Spec:** `docs/superpowers/specs/2026-05-30-x-source-design.md`

## Steps

1. **Failing tests** — add `tests/test_x.py` (mirror `test_openai.py`).
   Run, confirm fail (module missing).
2. **Implement** — add `feedy/sources/x.py` with `XSource` and `_parse_date(el)`
   helper. Card `a[href*="/en/blog/"]`, title `h3`, date `time`.
3. **Register** — `"x": XSource` in `_build_sources`; add `"x"` to
   `DEFAULT_SOURCES`; update `test_config.py` default assertions.
4. **Verify** — run full suite, confirm green.
5. **Roadmap** — mark Day 06 done.

## Commit sequence

- `test(x): add failing tests for XSource`
- `feat(x): scrape developer.x.com blog posts`
- `feat(cli): register x source`
- `chore: mark Day 06 complete`
