# Copy to .env or export directly. Values are read from the environment.

# Required only when summarisation is enabled.
ANTHROPIC_API_KEY=

# Required only when Slack delivery is configured.
FEEDY_SLACK_WEBHOOK=
